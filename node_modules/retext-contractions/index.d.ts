/**
 * Plugin to check contractions use.
 *
 * @type {import('unified').Plugin<[Options?], Root>}
 */
export default function retextContractions(
  options?: Options | undefined,
):
  | void
  | import("unified").Transformer<import("nlcst").Root, import("nlcst").Root>;
export type Root = import("nlcst").Root;
export type Sentence = import("nlcst").Sentence;
/**
 * Configuration.
 */
export type Options = {
  /**
   * Suggest straight (`'`) instead of smart (`’`) apostrophes.
   * Use `retext-quotes` if you want to properly check that though.
   */
  allowLiterals?: boolean | undefined;
  /**
   * Include literal phrases.
   * The default is to ignore them.
   */
  straight?: boolean | undefined;
};
